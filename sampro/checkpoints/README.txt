SAM 权重文件放置说明
======================

应用启动时会在此目录查找 `sam2.1_hiera_large.pt` 等 SAM 模型权重文件。如果目录为空或权重缺失，
请将官方或自定义训练得到的 `.pt`/`.pth` 文件复制到这里，或在程序内通过“File -> 选择 SAM 模型文件”指向其他路径。

Windows 发布版解压后的目录结构中同样包含 `sampro/checkpoints/` 文件夹，可直接用新的模型文件替换或更新。
